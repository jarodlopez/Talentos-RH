/**
 * Re-export del hook useAuth para respetar la estructura de carpetas
 * del plan (hooks/). La implementación vive junto al AuthProvider.
 */
export { useAuth } from "@/components/auth/AuthProvider";
